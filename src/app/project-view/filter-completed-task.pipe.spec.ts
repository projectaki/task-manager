import { FilterCompletedTaskPipe } from './filter-completed-task.pipe';

describe('FilterCompletedTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompletedTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
